# 基础功能升级验收 · 2026-09-08

下文 `../../tmp/` 路径为原开发工作区中的本机验收材料；日志、结果包及数据备份未纳入 GitHub 仓库。

## 本轮范围

原生 SwiftUI + MapKit：新建草稿、文字与地点名称编辑、自己的内容软删除与恢复、收藏/自己的内容/打开历史分类及搜索、真实地图选点、前台 GPS 漫游。3D 资产与云端建模暂缓；未改动旧 I WAS HERE 工程和科研目录。

数据格式从 v1 在内存迁到 v2，第一次成功事务才原子写回。正文、收藏、打开、回应、伙伴选择均保留。回收站不物理删除记录；失败时不假报保存成功。旧记录没有打开日期，显示“之前打开”。

## 已完成的检查

- 最终 GPS 版 XCTest：28/28 通过（12 定位规则与服务生命周期、9 编辑迁移草稿、7 原有存储）；日志 `../../tmp/pocket-world-gps-tests.log`，结果包 `../../tmp/pocket-world-gps-tests-20260908.xcresult`。定位测试使用合成坐标、伪管理器与独立生命周期通知，未请求真实位置或更改权限。
- 第一轮 XCTest：21/21 通过（7 原有存储、9 编辑迁移草稿、5 定位规则）；日志 `../../tmp/pocket-world-foundation-tests.log`，结果包 `../../tmp/pocket-world-foundation-tests-20260908.xcresult`。
- 存储覆盖：旧版迁移、伪造示范记录不能编辑、长度/坐标校验、删除隐藏/完整恢复、打开历史不重复记时、草稿未完成与发布原子清除、写入失败回滚、损坏/未来版本不覆盖。
- iPhone 17 模拟器实际操作：新建草稿中输入标题与中文正文；保存草稿退出；重启后恢复同一标题、正文和坐标；发布后地图 5→6；打开与收藏；编辑中文标题后详情即时显示新标题；再次启动后标题/正文/收藏保留。
- 查看了真实原生首页、详情截图，系统状态栏、安全区、地图和按钮布局正常。
- 发现并修复：切 tab 丢失地图中心；草稿仅改变类型/地点时未保存；详情未保存回应的关闭保护；首次自动保存后被误标为“续写上次草稿”。

- 最终构建 `../../tmp/pocket-world-gps-final-build.log`：BUILD SUCCEEDED，包含最后一处详情定位质量提示。

## 仍未完成的人工验收

此前启动时曾遇到 simctl 设备 Shutdown、UI 自动化状态改变提示，原因未确证，没有清空模拟器或删除数据。后续已成功启动原生 App，并在 16:52–16:53 的实际截图中确认 UNSW 示范道路和定位模式下的旧金山道路均正常显示。旧金山为模拟器提供的测试定位，不是用户 iPhone 的实测位置；本轮未更改模拟器坐标或点击新的定位许可。回收站 UI 的完整移入/恢复、搜索输入、未保存回应保护、连续移动时地图跟随仍需人工验收；其数据层、连续更新、生命周期和到达规则已有自动测试。真实 iPhone 步行和耗电未验收。

## 底图留白排查与恢复入口

用户 15:40 截图中只有 MapKit 网格、定位标记与高德归属文字。检查两处 Map 代码未发现遮挡底图的全幅背景。重新启动后，示范模式和设备定位模式均加载出道路；无法仅据这次恢复断言原始原因，也不能保证地图服务不再暂时失败。未重置地图缓存、清空模拟器或修改网络代理。

发现页和选点页增加显式重新载入按钮，只重建地图视图，保存当前相机位置和缩放。SwiftUI Map 没有公开瓦片加载/错误回调，因此未用 GPS 成功、相机回调或定时器虚报“底图加载成功”。地图徽标改为当前视野内的故事数量；附近卡片仍使用约 2 公里范围。去掉位置标记的重复系统标题。

模拟器检查：发现页旧金山道路可见，徽标“此处 0 份”；点击重载后同一街道、中心与缩放保留。新建页道路可见，点击重载后选点坐标显示不变，未输入/发布新内容，取消正常返回首页。拖动自动化未产生可观察的平移，故不把平移后的重载或任意缩放保留记为人工通过。构建日志 `../../tmp/pocket-world-map-recovery-build.log`；本轮未改存储或定位服务逻辑，未重复此前 28 项 XCTest。

## 体验数据与保护

新增的专用测试记录标题为「测试：恢复后还在的小美好」，正文「今天遇见了一点小美好。」；已打开并收藏，当前仍保留方便复查。此前用户记录未编辑/删除。升级前记录备份：`../../tmp/pocket-world-backups/world-state-before-foundation-ui.json`；源码备份：`../../tmp/pocket-world-backups/before-foundation-20260908.tar.gz`。

## 功能边界

没有服务器、跨用户内容、账户、联网同步、图片/音频发布或分发上架。实地地图只有本机真实坐标记录，示范种子独立展示。3D、喂食成长奖励仍是后续工作。
